# Free and open-source tooling: alternatives to paid tools and purchased equipment

This repo's evidence hierarchy ranks results by provenance — measured beats validated
simulation beats deterministic calculation beats manufacturer spec beats authoritative
reference beats internal history beats web material beats LLM inference — and every
significant value the agent states must carry one of those provenance tags back to a
traceable source. That stance extends to tooling: before a paid solver license, a
purchased instrument, or a paid data subscription is treated as the only way to produce
evidence at a given tier, this document exists to identify what free, open-source, or
free-of-charge external alternative already covers the same ground, at what fidelity,
and with what license obligations. It was compiled per an explicit request to survey
those alternatives across six tooling categories, using primary sources (project
repositories, license files, vendor developer docs) rather than secondhand summaries.
Every claim below is either sourced inline or flagged where two research passes
disagreed — nothing here is presented as settled that wasn't independently confirmed.

## Status: this is a research snapshot, and adapters have been built since

Read the "Fit for this repo" assessments below as the record of *why* each tool was
chosen, written before the adapters existed — not as a current inventory. Their
forward-looking phrasing ("would need a new `simulation/meep.py` subclass", "natural
`simulation/openparem.py` candidate", "no existing `simulation/elmer.py`") was accurate
when researched and is deliberately preserved rather than rewritten, because the
reasoning is the value here and editing it away would leave the conclusions with no
visible basis.

Built since this survey was compiled: `simulation/meep.py`, `simulation/elmer.py`,
`simulation/palace.py`, `simulation/openparem.py`, `simulation/gprmax.py`,
`simulation/qucs.py`, `simulation/ngspice.py`, `simulation/xyce.py`,
`simulation/ltspice.py`, `simulation/kicad_gerber2ems.py`, plus
`geometry/unit_cell.py` and `geometry/freecad_curved.py`. `simulation/hfss.py` is also
real, working code — license-confined to a controlled AEDT workstation, and per
ADR-0012 not on this project's default free/OSS path.

Still genuinely unbuilt, as of this note: FEniCSx/DOLFINx and the openEMS-CUDA fork
(neither recommended below), Sonnet Lite (not automatable), and
`rf_tools/filter_synthesis.py` (the closed-form filter-prototype synthesis identified
as absent from both scikit-rf and this repo).

## What's actually paid, and what runs without any LLM at all

Two axes worth separating out explicitly, since both get lost inside the long tool
tables below.

**Paid (costs money) — everywhere in this whole survey, only three things:**

| Item | Where it's actually useful despite the cost |
|---|---|
| Ansys AEDT/HFSS (`simulation/hfss.py`) | For a team that *already holds* an AEDT license: more mature antenna-specific post-processing (far-field, gain, efficiency) and a longer validation track record than openEMS/NEC2++. Deliberately off this repo's default path (ADR-0012) — the free/OSS stack is a complete, working substitute, not a stand-in waiting to be replaced. |
| Keysight ADS | No adapter exists here at all — ngspice/Xyce/Qucs-S/LTspice together cover its circuit-level role. ADS's edge is schematic-to-layout Momentum-class planar EM co-simulation, which nothing free/OSS in this survey fully replaces; only worth it for that specific gap. |
| IEEE Xplore | Standards/papers access. Only relevant if the owner already has employer/university access — no free-developer tier exists the way Digi-Key/Mouser/3GPP/FCC have. |

Everything else in this document — every simulator, every geometry tool, every
knowledge source, every local-LLM backend — is free-of-charge (OSS or proprietary
freeware) with **no purchase required at any point**, per ADR-0012's explicit decision
to keep paid EDA tooling off the default path and physical hardware out of the
codebase entirely.

**Non-LLM usage — the deterministic engineering toolkit runs with zero AI model calls:**

Every `simulation/*.py` adapter, `rf_tools/calculations.py`, `geometry/*.py`, and
`rf_tools/correlation.py` is a plain deterministic function or subprocess call — write
a netlist/deck/config, shell out to a real solver binary (or call a real library), parse
structured results back out. None of that path touches an LLM. The `CALCULATED` and
`SIMULATED` provenance tiers in this repo's evidence hierarchy exist specifically
*because* those results come from deterministic math and physics solvers, not model
inference — that's the whole reason they outrank `LLM_INFERENCE` in the ranking at the
top of this document. A caller could drive every solver adapter and calculation
directly (bypassing `agent/main.py` and `mcp_server/server.py` entirely) and get
identical results with no LLM configured at all.

The LLM only enters at two specific seams, both configured via `LLM_PROVIDER` /
`DEFAULT_LLM_BACKEND` (README's "LLM Configuration" section, ADR-0004): (1) the agent's
own reasoning/tool-orchestration loop (`agent/main.py`, `openai-agents`) — deciding
*which* deterministic tool to call and how to interpret/report the result; (2)
knowledge-base document extraction and embedding (`knowledge/extraction_llm.py`,
`knowledge/embedding.py`) — turning an ingested datasheet/standard/paper into
structured fields and vectors. Everything in "Self-hosted / free LLM & embedding
backends" below (Ollama, llama.cpp, LM Studio, HF TEI) is about *which* backend answers
those two seams, not about whether a given simulator/calculation result required one —
none of them do.

## Already free/open-source in this repo

Context, not the deliverable — so the rest of this document doesn't re-suggest adding
something that's already wired up.

| Component | What it's for | License |
|---|---|---|
| `openai-agents` | Agent orchestration/LLM tool-calling SDK. Defaults to the paid OpenAI API; ADR-0004's self-hosted backend (`LOCAL_LLM_BASE_URL`) makes it free once paired with a local OpenAI-compatible server — see the self-hosted LLM section below. | MIT |
| `mcp` | Tool-exposure protocol backing `mcp_server/`. | MIT |
| `scikit-rf` | RF network analysis/synthesis — Touchstone I/O, S/Z/Y/ABCD conversion, `Circuit`/`Media` matching-network primitives. | BSD-3-Clause |
| `numpy`, `scipy`, `pandas`, `matplotlib` | Numerical/plotting stack. `optimization/` (`grid.py`, `gradient.py`, `genetic.py`, `bayesian.py`, `sweep.py`) is already built entirely on `scipy.optimize` (`differential_evolution`, `minimize`) and `numpy` — confirmed by reading every file in `optimization/`. There is no paid optimization tool being used or needing replacement here; DEAP, pymoo, and Optuna (all free/OSS — Apache-2.0, LGPL-3.0, and MIT respectively) would only add multi-objective/Pareto-front algorithm variety scipy.optimize doesn't have, if that's ever wanted. Not a gap. | BSD-3-Clause |
| `pydantic` | Data validation. | MIT |
| `psycopg[binary]` + `pgvector` | Postgres driver and vector-similarity extension backing the knowledge base. | LGPL-3.0-only (confirmed from `psycopg/psycopg`'s own `LICENSE.txt`) / PostgreSQL-style permissive |
| `python-dotenv` | `.env` loading. | BSD-3-Clause |
| `rich` | Terminal output. | MIT |
| `docling` | PDF/document parsing for datasheet ingestion (`knowledge/extraction.py`). | MIT (confirmed from `docling-project/docling`'s own `LICENSE` file) |
| NEC2++ | Method-of-moments antenna solver (`simulation/nec2pp.py`); manual binary install, not automated by this repo. | GPL |
| openEMS (+ AppCSXCAD) | FDTD full-wave EM solver (`simulation/openems.py`); manual binary install. | GPLv3 |

## Full-wave EM / antenna simulation (alternatives/supplements to Ansys HFSS, CST Studio Suite)

| Tool | Replaces/supplements | License | Hardware required? | Primary source | Fit for this repo |
|---|---|---|---|---|---|
| MEEP | Supplements openEMS — general FDTD Maxwell solver, MIT NanoComp | GPLv2 (confirmed from the project's own `LICENSE`) | No | [Intro](https://meep.readthedocs.io/en/latest/Introduction/), [Units doc](https://github.com/NanoComp/meep/blob/master/doc/docs/Units_and_Nonlinearity.md) | Nanophotonics-first tool, legitimately retargetable to RF/antenna scale because Maxwell's equations are dimensionless/scale-invariant in Meep's own unit system. Best fit: a second FDTD cross-check engine, or specifically periodic/metamaterial unit-cell sweeps — its native Bloch-periodic boundaries and eigenmode-source tooling are more mature out of the box than openEMS's for that geometry class. No official GPU engine (CPU/MPI/SIMD only); no packaged microstrip/coax port helper the way openEMS's Python interface has, so ports and NF2FF post-processing are hand-built. Would need a new `simulation/meep.py` `Simulator` subclass. |
| Elmer FEM (`VectorHelmholtz` module) | Supplements — FEM full-wave capability distinct from Elmer's more commonly cited low-frequency EM modules | **Flagged license discrepancy**: core `ElmerSolver` library is LGPL, but the exact point version disagrees between two primary sources this research read directly — the repo's own `license_texts/ElmerLicensePolicy.md` says LGPL **v2.1**, while `elmerfem.org`'s own license blog post says LGPL **v2.0**. Both agree the physics solver modules (including `VectorHelmholtz.F90`) and ElmerGUI/ElmerGrid/ElmerParam are GPL v2/v3. Verify the exact core-library point version against `COPYING` before recording it in `docs/LICENSE_MATRIX.md`; it doesn't change the free/OSS conclusion, only the citation. | No | [VectorHelmholtz.F90](https://github.com/ElmerCSC/elmerfem/blob/devel/fem/src/modules/VectorHelmholtz.F90), [license policy](https://github.com/ElmerCSC/elmerfem/blob/devel/license_texts/ElmerLicensePolicy.md), [license blog](https://www.elmerfem.org/blog/license/) | Elmer's primary user base and most of its modules are structural/CFD/heat-transfer/low-frequency EM; `VectorHelmholtz` is real but less mature and has no antenna-specific S-parameter/far-field/gain post-processing. No existing `simulation/elmer.py`; building one needs external meshing (Gmsh/ElmerGrid) plus hand-assembled ports. Lower priority than MEEP/Palace/OpenParEM for the metamaterial-unit-cell gap; more relevant if a future task needs coupled EM+thermal/structural multiphysics (e.g. a mounted "adaptive EM skin"). Reachable today via EMStudio (see geometry section) without writing a dedicated adapter. |
| FEniCSx / DOLFINx | Supplements — general-purpose FEM PDE library with official time-harmonic Maxwell demos | LGPLv3-or-later (confirmed from `FEniCS/dolfinx`'s `COPYING.LESSER`) | No | [scattering demo](https://docs.fenicsproject.org/dolfinx/v0.7.2/python/demos/demo_scattering_boundary_conditions.html), [waveguide demo](https://docs.fenicsproject.org/dolfinx/v0.7.2/python/demos/demo_half_loaded_waveguide.html) | Genuinely usable for RF-relevant full-wave Maxwell problems (validated against Harrington's analytical waveguide solutions), but ships no antenna-specific geometry/port/far-field abstractions at all — the user writes the weak-form UFL directly. Steepest learning curve and least RF-specific tooling in this report; a Maxwell-solver construction kit, not an HFSS-class application. Only worth adopting for a bespoke metamaterial eigenproblem not well served by openEMS/MEEP/Palace, and only if the team is prepared to implement the physics formulation from scratch. |
| Sonnet Lite | Supplements — free-of-charge, memory-capped 2.5D planar MoM tool | Free-of-charge proprietary freeware (confirmed from Sonnet's own product page/FAQ — "it's FREE!"). **Not** OSI-approved open source; no source availability. | No (Windows-only PC, no dedicated GPU; 2 GB RAM / 1 GB disk minimum) | [product page](https://www.sonnetsoftware.com/products/lite/), [FAQ](https://www.sonnetsoftware.com/products/lite/faq.html) | Windows-only interactive GUI with no documented CLI/API — not automatable into this repo's subprocess-adapter pattern. 32 MB registered memory cap and 2.5D-planar-only (no true 3D volumetric modeling), so it can't address this repo's 3D conformal-antenna/metamaterial-unit-cell target the way openEMS/NEC2++/MEEP/Palace/OpenParEM can. Value here is as a manual, out-of-band cross-check on planar feed-network/patch designs, not an agent-invoked tool. |
| openEMS-CUDA (unofficial GPU fork) | Supplements — experimental CUDA-accelerated EC-FDTD engine | GPLv3 (inherited from openEMS, per the fork's own README) | Yes — needs an NVIDIA CUDA GPU to exercise its distinguishing feature (the owner's RTX 5080 qualifies; no new purchase implied) | [repo](https://github.com/aWZHY0yQH81uOYvH/openEMS-CUDA), [upstream discussion #154](https://github.com/thliebig/openEMS-Project/discussions/154) | Confirms mainline `thliebig/openEMS` — the version `simulation/openems.py` already targets — has no merged official GPU engine (SIMD/multithread/MPI only per upstream's own GitHub Discussions). This fork is an unmerged, self-described "experimental" third-party effort with no indication of maintenance parity with mainline. Not recommended for integration as-is; worth re-checking periodically for a merged official GPU path rather than adopting this fork now. |
| Palace (AWS) | Supplements — parallel 3D FEM full-wave solver on MFEM/libCEED, with native Floquet/periodic-port support | Apache-2.0 (confirmed from `awslabs/palace`'s own `LICENSE`) | No (CPU-only from laptop to HPC scale; CUDA/HIP GPU acceleration is optional) | [docs](https://awslabs.github.io/palace/stable/), [repo](https://github.com/awslabs/palace) | The single most directly relevant finding across this whole EM-simulation research pass for CONTEXT.md's open metamaterial-unit-cell gap: Palace's own docs include a "Floquet Ports for a Dielectric Grating" example — exactly the periodic-boundary machinery a unit-cell simulation needs, which neither the existing NEC2++ (MoM) nor openEMS (FDTD, no native Floquet port) adapter exposes today. FEM rather than FDTD/MoM, so it's a genuine independent-method cross-check, not a duplicate. CLI-driven from JSON config + mesh files — a natural `simulation/palace.py` `Simulator` subclass, same subprocess shape as the existing adapters. Younger project with less RF/antenna validation history than openEMS/HFSS (example gallery skews toward its original superconducting-qubit use case); verify far-field/gain post-processing against a known case before trusting design decisions. Also the only permissively-licensed (non-copyleft) full-wave option in this report, relevant if GPL obligations on a given deployment are a concern. |
| OpenParEM (OpenParEM2D + OpenParEM3D) | Supplements — purpose-built frequency-domain FEM solver with antenna-specific S-parameters *and* far-field gain/directivity/efficiency from one solve | GPLv3-or-later (confirmed from `OpenParEM/OpenParEM`'s own `LICENSE`) | No | [site](https://openparem.org/), [repo](https://github.com/OpenParEM/OpenParEM) | The closest free/OSS functional match in this whole report to what HFSS is actually used for in antenna work — S-parameters and far-field gain/directivity/efficiency from the same 3D solve. Natural `simulation/openparem.py` candidate and a good FEM cross-check partner for the existing FDTD (openEMS) and MoM (NEC2++) adapters, also relevant to the simulation/measurement-correlation gap once a VNA measurement exists to compare against. Young (v1.0.1 Sept 2024, v2.0 with antenna metrics Mar 2025) — far less battle-tested than HFSS/openEMS/NEC2++. CLI/mesh-file-driven; no Python API found. |
| gprMax | Supplements — FDTD solver built for ground-penetrating radar, general enough for free-space Maxwell problems | GPLv3-or-later (same copyleft family as openEMS) | No (optional CUDA GPU path; owner's RTX 5080 already covers it, CPU/OpenMP path needs nothing special) | [repo](https://github.com/gprMax/gprMax) | Pure Python/Cython, pip-installable — lower integration overhead than openEMS's CSXCAD/AppCSXCAD toolchain; a `simulation/gprmax.py` adapter would follow the same subprocess pattern as the existing ones, feeding its own `.in` geometry format. Purpose-built for GPR (dielectric half-space, ground-coupled antennas near lossy soil); its bundled antenna models proxy commercial GPR hardware, not general free-space antennas — would need real adaptation, not drop-in use, for this repo's free-space/conformal designs. |
| Gmsh | Not a simulator itself — the mesh generator two of the solvers above (Elmer FEM, OpenParEM3D) require as an upstream step, since neither ships its own meshing | GPL-2.0-or-later, with an explicit linking exception permitting combination with Netgen/METIS/OpenCASCADE/ParaView code under their own licenses (confirmed from gmsh.info's own `LICENSE.txt`) | No | [site](https://gmsh.info/), [license](https://gmsh.info/LICENSE.txt) | Genuinely load-bearing, not optional: `simulation/elmer.py` shells out to it directly (`gmsh file.geo -3 -format msh2 -o file.msh`) to mesh a `.geo` script (built with Gmsh's own OpenCASCADE-kernel scripting) before handing the result to ElmerGrid, and `simulation/openparem.py` requires an already-meshed Gmsh `msh22` file as its `mesh_file` input (mesh generation is explicitly out of scope for that adapter — "the user is responsible for" the FreeCAD + Gmsh + ParaView flow, per that module's own docstring). **Palace does not use it** — `simulation/palace.py` reads its own native MFEM mesh format, not Gmsh's — worth stating explicitly since Gmsh's presence in the Docker image could otherwise read as "the mesh tool for every FEM solver here," which it isn't. CLI-only, no GUI dependency for this repo's headless use; confirmed genuinely apt-installable on Ubuntu 24.04/26.04 (unlike FreeCAD, which needed a PPA) during this repo's own Docker build. |

## Circuit-level RF/microwave simulation (alternative to Keysight ADS)

| Tool | Replaces/supplements | License | Hardware required? | Primary source | Fit for this repo |
|---|---|---|---|---|---|
| ngspice | ADS's transistor-level DC/AC/transient/noise simulation | Modified BSD is the default per the project's own `COPYING`, but ngspice merges several upstream codebases with heterogeneous per-file licenses: spice3f5/CIDER = modified BSD, XSPICE = public domain (except one GPLv2-or-later table module), `tclspice.c` = LGPLv2, `maths/sparse` = MIT-style, ADMS-generated code = LGPLv2.1, `numparam` = LGPLv2-or-later. All OSI-approved-license territory (BSD/MIT/GPL/LGPL family), but not a single uniform license — matters if this repo ever redistributes ngspice source rather than invoking an installed binary. | No | [site](https://ngspice.sourceforge.io/), [COPYING](https://github.com/ngspice/ngspice/blob/master/COPYING) | Fits the exact adapter shape `simulation/nec2pp.py`/`simulation/openems.py` already use: a `Simulator` subclass writes a deterministic SPICE netlist, invokes `ngspice -b -r out.raw deck.cir` as a subprocess, parses the rawfile. Ships as a system binary (like NEC2++/openEMS), not a PyPI package — manual install per this repo's existing convention, not a `pyproject.toml` extra. Native transmission-line/microstrip modeling is basic and full multi-port S-parameter analysis isn't first-class (Touchstone export via `.measure` works, but coupled lines/dispersion/EM-accurate microstrip need community XSPICE extensions or a companion full-wave tool — this repo already has NEC2++/openEMS for that role). Fills the "nonlinear/active-device circuit-level" gap next to the existing full-wave adapters; not an EM-accurate microstrip/filter tool by itself. |
| Xyce | ADS's large-scale/parallel nonlinear circuit simulation, including harmonic balance | GPLv3, confirmed on the public `Xyce/Xyce` GitHub release. Sandia's own FAQ notes some internal Xyce builds are distributed to partner labs under other, non-GPL terms — the public GitHub/`xyce.sandia.gov` release specifically is GPLv3. No explicit ECCN/EAR/ITAR marking was found on the public release (SAND reports are marked "Unlimited Release"; the repo carries a standard NNSA/DOE contract notice, not an export-control legend) — record as "no marking found; verify before restricted-data use," not a clean bill of health. | No (MPI parallel scaling is optional, not required) | [site](https://xyce.sandia.gov/), [repo](https://github.com/Xyce/Xyce), [FAQ](https://xyce.sandia.gov/documentation-tutorials/frequently-asked-questions/) | Command-line only by design (no GUI, per Sandia's own FAQ) — slots into the `Simulator` subclass pattern even more directly than ngspice: write netlist, `Xyce netlist.cir`, parse `.PRINT` output. Close-enough SPICE3F5-style netlist syntax to ngspice that a shared `simulation/spice_common.py` netlist/output helper is worth factoring if both adapters get built (check `simulation/base.py` and `nec2pp.py`'s existing helpers first, per this repo's search-before-write convention). Same category gap as ngspice — general nonlinear circuit simulator, no S-parameter/microstrip synthesis comparable to ADS Momentum. Its edge is harmonic-balance and large-circuit parallel scaling, unlikely to matter at this repo's likely matching-network/small-MMIC circuit sizes — ngspice is the lighter first target, Xyce a secondary option. |
| Qucs-S (Qucsator backend) | ADS's schematic-level circuit simulation | GPL-2.0-or-later (confirmed via the explicit SPDX identifier in `ra3xdh/qucs_s`'s `COPYING`) | No | [repo](https://github.com/ra3xdh/qucs_s) | **Flagged direct contradiction between the two research passes that covered Qucs-S, on a fact that determines whether an adapter should be built at all.** One pass reports Qucs-S's schematic-capture GUI and F2-triggered simulation run entirely inside the GUI event loop with no documented headless CLI/batch flag, that the one third-party automation wrapper found (`zonca/python-qucs`) targets the legacy pre-fork Qucs/Qucsator engine and is archived/unmaintained, and that Qucs-S's own docs state multi-port S-parameter RF simulation is **not implemented in Qucs-S itself** (deferred to the standalone legacy Qucs/Qucsator) — concluding no `simulation/qucs_s.py` adapter should be built, since a headless ngspice/Xyce adapter already obtains everything Qucs-S would add. A second pass reports the opposite: that the `qucsator` binary "runs headless from a netlist file (`qucsator -i in.net -o out.dat`)" and companion tools (`qucs -n`, `qucsconv`) convert schematics/SPICE netlists into that format, "fully drivable from the command line/subprocess" — and recommends building a `simulation/qucs.py` adapter as the ADS substitute this repo's README already tracks by license but has no code for. This is not a license disagreement (both agree GPL-2.0-family) — it's a functional disagreement about whether `qucsator` has a working headless CLI and native S-parameter capability, which the two passes reach opposite conclusions on. **Do not commit to either building or skipping a Qucs-S adapter based on this report alone** — hand-verify the installed `qucsator` binary's actual `-i`/`-o` behavior and S-parameter output before deciding; ngspice/Xyce remain the safer first targets regardless of how this resolves. |
| LTspice | Supplements — free SPICE simulator with an extensive vendor device-model library | Free-of-charge proprietary freeware (Analog Devices SLA 20191031-LTS-CTSLA) — non-exclusive, non-transferable, internal-use grant; explicitly prohibits reverse engineering/decompiling; reciprocal patent-license clause back to ADI. Closed source — **not** OSI-approved, distinct from the GPL/BSD tools above. | No (Windows-native; runs under Wine on Linux, or ADI's macOS build) | [product page](https://www.analog.com/en/resources/design-tools-and-calculators/ltspice-simulator.html), [license](https://ltwiki.org/files/LTspiceHelp.chm/html/License.pdf) | Has command-line batch switches and third-party Python drivers (PyLTSpice/spicelib) that subprocess it and parse `.raw` output — technically automatable, unlike Qucs-S's GUI. But it's closed-source, Windows-GUI-app-wrapped-via-flags rather than a native headless binary like ngspice/Xyce, and this repo prefers free/OSS, Linux/CI-friendly tooling — worse fit than ngspice/Xyce as an integration target. No RF-specific advantage over them (same general-SPICE limitation on distributed/EM structures); its real strength is the vendor model library and universal familiarity, not simulation method. Reported here rather than omitted so the survey is honest about what exists. |

scikit-rf (already a core dependency — see the table above) already covers part of ADS's linear-synthesis role: `skrf.media.Media` gives L-network impedance-matching synthesis and `skrf.circuit.Circuit` gives ladder-network assembly from S/ABCD building blocks. The one confirmed gap — verified by grepping `rf_tools/` and the whole tree for `chebyshev`/`butterworth`/`ladder`/`g_values`/`prototype` — is classical filter-prototype synthesis (order/ripple/cutoff → g-value table → assembled ladder); scikit-rf has no such function and neither does this repo. That's closed-form, deterministic math and belongs in new first-party code (see "What has to be custom-built" below), not a new external circuit simulator.

## Software-defined / simulated instrument backends (alternatives to a real VNA, spectrum analyzer, signal generator, power meter)

| Tool | Replaces/supplements | License | Hardware required? | Primary source | Fit for this repo |
|---|---|---|---|---|---|
| PyVISA-sim | A real VNA/spectrum analyzer/signal generator/power meter — simulates SCPI/VISA instruments entirely in software from a YAML device definition | MIT (confirmed both from the PyPI classifier and by reading `pyvisa/pyvisa-sim`'s own `LICENSE.txt` directly) | No — zero hardware of any kind; the one component in this whole report that lets `measurement/vna.py` etc. run end-to-end with no lab bench | [repo](https://github.com/pyvisa/pyvisa-sim), [docs](https://pyvisa.readthedocs.io/projects/pyvisa-sim/en/latest/) | Confirmed by reading the code directly: all four adapters (`measurement/vna.py:253`, `spectrum_analyzer.py:205`, `signal_generator.py:191`, `power_meter.py:165`) call the identical bare `pyvisa.ResourceManager()`. PyVISA's own backend-selection docs say a bare call honors the `PYVISA_LIBRARY` env var — so `PYVISA_LIBRARY=@sim` (or `path/to/fixture.yaml@sim`) makes every adapter transparently open pyvisa-sim instead of a real VISA library, with **zero adapter code changes**. The bundled `default.yaml` only implements a toy signal-generator command set, not this repo's actual SCPI vocabulary (`*IDN?`, `SENS:FREQ:STAR`, `CALC:DATA? SDATA`, etc. from `DEFAULT_SCPI_COMMANDS`) — a repo-specific YAML fixture matching those exact strings is a small, genuinely new deliverable (see "What has to be custom-built"). Does not touch or weaken `measurement/base.py`'s human-approval gate — `request_physical_measurement_approval()` still requires an explicit `approval_callback`, same as the real-hardware path. |
| PyVISA-py | The paid/proprietary NI-VISA or Keysight IO Libraries VISA *runtime* — not a substitute for real hardware itself | MIT (PyPI "OSI Approved :: MIT License" classifier, PyVISA-py 0.8.1) | Yes for actual use — it's a VISA transport implementation, not a simulator, and still needs a genuine instrument over USB/GPIB/serial/TCPIP. Installs and imports today with zero hardware; a bare `pyvisa.ResourceManager()` automatically falls back to this backend whenever no proprietary IVI library is installed, which changes `_real_pyvisa_importable()`'s behavior in this repo's dev/CI environment even before any hardware is attached. | [repo](https://github.com/pyvisa/pyvisa-py), [docs](https://pyvisa.readthedocs.io/projects/pyvisa-py/en/latest/) | No code change needed — same bare `ResourceManager()` call every adapter already makes, either via the no-IVI-installed fallback or explicit `PYVISA_LIBRARY=@py`. Worth adding to the `measurement` extra alongside `pyvisa` as the documented free path, distinct from the pyvisa-sim path (which needs no hardware at all) and from NI-VISA/Keysight IO Libraries (proprietary). GPIB support specifically depends on `linux-gpib` (Linux) or `gpib-ctypes` (Windows, wraps a vendor DLL) — not fully vendor-free for GPIB; USB/TCPIP/serial paths are fully free. |
| GNU Radio (simulation/loopback workflows) | Not a substitute for the SCPI/VISA adapter contract — a separate, additive way to generate/filter/spectrum-analyze synthetic RF/baseband signals with zero SDR hardware | GPL-3.0-or-later for the shipping GR3 series (confirmed from `gnuradio/gnuradio`'s `COPYING`/SPDX metadata); the in-development GR4 line has announced an intent to move newly-written core code to MIT, but the installable version today is GPLv3. | No — for the pure-software blocks (signal/noise sources, `file_source`/`file_sink`, filters, FFT/waterfall sinks, ZeroMQ PUB/SUB loopback between two flowgraphs). Hardware-only blocks (UHD/USRP, gr-osmosdr, PlutoSDR/IIO) require the corresponding real SDR — that boundary is block-level, not a toolkit-wide toggle. | [FAQ](https://wiki.gnuradio.org/index.php/FAQ), [NBFM simulation example](https://wiki.gnuradio.org/index.php/Simulation_example:_Narrowband_FM_transceiver) | Not a plug-in for `Instrument.measure()`/`check_physical_actuation_gate()` — GNU Radio flowgraphs speak a streaming interface, not SCPI/VISA. Relevant to CONTEXT.md's simulation/measurement-correlation gap and general DSP-path validation as a free, zero-hardware synthetic "measured" signal source (built via `gr.top_block()`, entirely file/ZMQ-based) to sanity-check `spectrum_analyzer.py`'s parsing logic against known synthetic waveforms before touching a real instrument — but that's new integration code (a small adapter translating GNU Radio's sample stream into this repo's result shapes), not a configuration-only fit the way pyvisa-sim is. |

## Antenna/metamaterial geometry generation & PCB layout (feeding `simulation/openems.py` and `simulation/nec2pp.py`)

| Tool | Replaces/supplements | License | Hardware required? | Primary source | Fit for this repo |
|---|---|---|---|---|---|
| FreeCAD | A parametric-CAD front end for antenna/unit-cell/conformal-host geometry (no paid CAD is used today; this displaces what would otherwise be a paid tool like SolidWorks) | LGPL-2.1 (confirmed via the official repo's license metadata) | No | [repo](https://github.com/FreeCAD/FreeCAD) | Runs genuinely headless via `FreeCADCmd`/`FreeCAD --console` (confirmed against FreeCAD's own docs) — a full Python interpreter, every module except `FreeCADGui`, suitable for CI/agent automation. `simulation/openems.py`'s `generate_openems_xml()` consumes a flat dict of Box/Cylinder primitives (`p1_m`/`p2_m` corners + optional radius); `simulation/nec2pp.py`'s `generate_nec2_deck()` consumes a `geometry['wires']` list. A headless FreeCADCmd script can build a parametric model and emit exactly this repo's geometry dict as JSON, for box/cylinder-approximable shapes with no STEP/mesh round-trip. No existing FreeCAD-to-this-repo's-schema translator — writing one is genuinely new code, roughly adapter-sized. No native CSXCAD/openEMS/NEC2 exporter of its own (the one community bridge, next row, is GUI-only and off-schema). |
| FreeCAD-OpenEMS-Export (`LubomirJagos`) | Prior art — existing community FreeCAD↔openEMS bridge | GPL-3.0 | No | [repo](https://github.com/LubomirJagos/FreeCAD-OpenEMS-Export) | GUI-macro only, per its own docs — no headless/scripted entry point, and its output is an Octave/Matlab script, not the FDTD-XML or geometry dict this repo's adapter uses. Not directly pluggable; useful only as prior art proving no ready-made "FreeCAD model → this repo's schema" tool exists, and as a reference for what mesh/port/material metadata a human-facing workflow needs. A maintained fork (`snhobbs/FreeCAD-FDTD-Workbench`) adds mesh-line visualization on the same GUI-macro model — same limitation. |
| CSXCAD + openEMS Python interface | openEMS's own first-party geometry-scripting companion — `CSXCAD` for programmatic geometry/material/mesh, `openEMS` (PyPI) for excitation/port/boundary/NF2FF setup | **Flagged license distinction, resolved but worth recording precisely**: one research pass described "CSXCAD + openEMS Python packages" as uniformly "same GPLv3 as openEMS" because both ship from the `thliebig/openEMS-Project` umbrella repo. A second, more specific pass read CSXCAD's own separate repo (`thliebig/CSXCAD`) directly and found **LGPLv3**, distinct from the `openEMS` PyPI package (bindings to the solver binary itself), which is GPLv3 like the solver. Both packages are real; record them as two separate license rows in `docs/LICENSE_MATRIX.md` — CSXCAD: LGPLv3, `openEMS` (Python bindings): GPLv3 — rather than folding CSXCAD under openEMS's GPLv3. | No | [CSXCAD repo](https://github.com/thliebig/CSXCAD), [openEMS Python docs](https://docs.openems.de/python/python.html) | Directly relevant to existing code: `simulation/openems.py` currently hand-writes FDTD-XML text by explicit design (per that module's own header comment, to avoid a compiled Python-extension dependency) and its `_primitive_xml()` implements only CSXCAD's Box and Cylinder tags — a real subset of what CSXCAD supports. The missing piece is `AddPolygon`/`AddLinPoly` — exactly what an arbitrary metamaterial-unit-cell shape (SRR, Jerusalem cross, non-rectilinear FSS element) needs, and which gdstk's polygon output (below) would feed directly. Two paths: extend `_primitive_xml()`'s shape branch with a `polygon` case matching CSXCAD's `<Polygon>` element while keeping the current zero-extra-dependency hand-XML approach, or add `CSXCAD` as a real optional dependency and build geometry through its native Python object model. Does not replace the geometry-*design* step (deciding a unit cell's shape/dimensions) — it's the authoring/serialization layer this repo's own code already partially reimplements. |
| gdstk (GDSII Tool Kit) | Periodic/planar metamaterial unit-cell array generator | Boost Software License 1.0 | No | [repo](https://github.com/heitzmann/gdstk) | Purpose-built for exactly the repeating-element layout metamaterial-unit-cell arrays need — `gdstk.Reference(cell, columns=N, rows=M, spacing=(dx,dy))` tiles a unit cell, `cell.get_polygons()` returns vertices directly in Python (pure in-memory, no GDSII file round-trip needed). A rectilinear unit cell's bounding rectangle maps straight onto the existing `box` primitive in `simulation/openems.py`'s geometry dict with zero adapter changes; a non-rectilinear unit cell (most real SRR/Jerusalem-cross designs) needs `_primitive_xml()`'s polygon extension (see CSXCAD row above). 2D-planar only — fits flat metamaterial-array/FSS/patch layers, not the curved conformal host-surface geometry the "adaptive EM skin" target also needs (pair with FreeCAD for that). |
| gdspy | Historical — gdstk's predecessor | Boost Software License 1.0 | No | [repo](https://github.com/heitzmann/gdspy) | Archived/read-only as of 2024-08-09, frozen at v1.6, with the project's own maintainers directing users to gdstk. No new work should target it; this repo has no existing gdspy code to migrate. Listed only to close out an explicit "gdstk and/or gdspy" research question. |
| KiCad | A planar PCB-antenna/feed-network layout front end — substitutes for a paid PCB-EDA suite plus a standalone RF transmission-line calculator | GPL-3.0-or-later (confirmed via the official repo's license metadata) | No | [repo](https://gitlab.com/kicad/code/kicad) | Not itself a source this repo scripts directly into openEMS/NEC2 primitives — it's a 2D copper-layer layout tool. Its real relevance is as the front end for planar PCB-etched antennas/metamaterial unit cells: a `pcbnew`/IPC-API script places footprints/tracks, KiCad's "Generate Fabrication Outputs" produces Gerbers, and gerber2ems (next row) consumes those to drive an actual openEMS run — that's the concrete integration path, not a direct KiCad-to-schema bridge. Its bundled microstrip/CPW/coax impedance calculator (Kirschning/Jansen equations) is a candidate deterministic cross-check for `rf_tools/calculations.py`, a separate research thread. Script against the new stable `kicad-python` IPC API, not the legacy `pcbnew` SWIG module (targeted for removal in KiCad 11). |
| gerber2ems (antmicro) | The concrete, ready-built KiCad-Gerber-to-openEMS geometry pipeline | Apache-2.0 | No | [repo](https://github.com/antmicro/gerber2ems) | The direct answer to "how would a KiCad-designed geometry plug into the existing openEMS adapter": rasterizes Gerber layers via `gerbv`, triangulates conductor geometry, auto-generates FDTD mesh and via/port placement from a stackup JSON, drives openEMS internally (via its own Python interface, **not** through this repo's `generate_openems_xml()`/geometry-dict schema), then FFTs the result into impedance/S-parameter CSVs. Would be a new `simulation/kicad_gerber2ems.py` adapter following the existing subprocess-and-`SimulationResult` pattern, but as a parallel geometry-input path specific to KiCad PCB structures — purpose-built for PCB signal-integrity work (trace impedance, via/stackup S-parameters), not antenna far-field/gain patterns. Depends on the external `gerbv` rasterizer (also FOSS) plus a working openEMS install. |
| gerbv | gerber2ems's own Gerber-rasterization dependency — not itself EDA/simulation software, a Gerber-file viewer/rasterizer | GPL-2.0 (confirmed from `gerbv/gerbv`'s own README/COPYING: "Gerbv and all associated files is placed under the GNU Public License (GPL) version 2.0"; bundles TinyScheme under BSD-3-Clause and dxflib under GPL-2.0-or-later as third-party components) | No | [repo](https://github.com/gerbv/gerbv) | Not a tool this repo scripts directly — it's what `gerber2ems` shells out to internally to rasterize KiCad's Gerber layers before triangulating conductor geometry. Listed as its own row (not folded into gerber2ems's) because it's a separately-licensed apt package this repo's Docker build actually installs (`apt-get install ... gerbv`, `Dockerfile`) as a `gerber2ems` runtime dependency, the same reasoning this document already applies to CSXCAD vs. openEMS. No direct integration code of this repo's own calls it — confirming it's present and on `PATH` is enough. |
| pyems (`matthuszagh`) | Secondary reference — a higher-level community wrapper around CSXCAD/openEMS's own interfaces, adding automatic FDTD mesh-line generation | GPL-3.0 | No | [repo](https://github.com/matthuszagh/pyems) | Not a geometry-generation source itself — consumes CSXCAD geometry, the same layer this repo already touches. Its most relevant piece is the automatic-mesh-generation algorithm (`simulation/openems.py`'s `geometry['mesh']` currently requires explicit caller-supplied mesh lines) — a candidate reference for a future auto-mesh helper, not something to depend on directly. Its `write_footprint()` helper exports a simulated structure *to* a KiCad footprint — the reverse direction from gerber2ems, listed for completeness. Single-maintainer project; verify current activity before depending on it as more than a reference implementation. |
| EMStudio (FreeCAD RF/EM workbench) | Antenna/RF geometry authoring otherwise done in HFSS or ADS's schematic-to-layout tools — directly targets CONTEXT.md's "antenna geometry generators" gap | LGPL-2.1-or-later for the EMStudio code itself (host app FreeCAD is LGPL-2.0-or-later). Each solver it drives keeps its own separate license (openEMS/NEC2/gprMax GPL-family, Palace Apache-2.0, Elmer mixed GPL/LGPL) since none are linked/bundled. The "EMStudio"/"AJJ3" name/logo/icon set is excluded from the LGPL grant per the project's own `TRADEMARK` file. | No | [repo](https://github.com/king-aj3/EMStudioFree) | A FreeCAD workbench (geometry → materials → ports/boundaries → mesh → solve → results) with a rule-based "Element Designer" that synthesizes dimensioned wire (dipole/monopole/folded), Yagi-Uda (validated against NBS TN-688 measured gains), microstrip patch (openEMS-verified), and LPDA antenna geometry from requirements, plus VLF/LF/MF small-antenna analytics and a cable designer — driving openEMS/NEC2/Elmer/Palace as isolated subprocesses rather than bundling their code. Directly targets the antenna-geometry-generator gap for *conventional* antenna families; its built-in families are not metamaterial unit cells, so SRR/Jerusalem-cross-style parametrization would still need bespoke FreeCAD Python scripting (see "What has to be custom-built"). It's a full desktop app, not pip-installable — integration is subprocess-based (`freecadcmd script.py`), same shape as the other simulator adapters. Young/small project (single visible maintainer) — verify maturity before depending on it beyond geometry export. |

## Component/manufacturer intelligence & external standards data (free tiers/public domain, not purchased data)

| Source | Replaces/supplements | License | Hardware required? | Primary source | Fit for this repo |
|---|---|---|---|---|---|
| Digi-Key Product Information API v4 | A paid Octopart/IHS Markit/Ultra Librarian subscription — component/manufacturer intelligence, direct from the distributor | Free developer-account tier (self-service OAuth2 registration, Sandbox before Production, no purchase). Proprietary API terms — "free" means no subscription fee, not open data. Exact rate limits for the free tier aren't published; confirm against the registered app's dashboard. | No | [developer docs](https://developer.digikey.com/products/product-information-v4) | Feeds `knowledge/ingest.py` as a new upstream fetch step: pull a part's datasheet PDF URL via the API, download it, call the existing `ingest_document(source_type='datasheet', ...)` path unchanged. New/custom code is the API client + URL-to-file step, not the ingestion pipeline itself. Returns catalog/stock data plus a datasheet link, not structured per-field electrical specs — `docling` still does the actual extraction. |
| Mouser Search API | Same gap as Digi-Key — an independent second distributor source | Free with a registered API key (proprietary terms at `mouser.com/en/apiterms/`, not an OSI license) — a data-access grant, not open data. | No | [API hub](https://www.mouser.com/en/api-hub/) | Same integration shape as Digi-Key: a thin client feeding datasheet URLs into the existing `ingest_document` path. Running both gives two independently-sourced confirmations for the same part — useful given this repo's provenance model treats extraction ambiguity as a first-class field (`INFERRED` vs. `MANUFACTURER-SPECIFIED`). |
| Nexar API (Octopart data) | The former standalone Octopart APIv4 (deprecated) and general paid cross-distributor aggregator subscriptions | Free tier genuinely still exists (confirmed as of this research) — free OAuth2 self-registration, evaluation app capped at roughly 1,000 matched parts (100 in the pre-signup Playground); higher volume needs a paid plan. Proprietary API terms, not OSI-licensed. | No | [nexar.com/api](https://nexar.com/api) | Same datasheet-URL-into-docling shape as Digi-Key/Mouser. Differentiator is cross-distributor aggregation in one query, but the matched-parts cap makes it better suited to occasional cross-checks than primary bulk ingestion — Digi-Key/Mouser are the likelier default bulk source, Nexar the multi-distributor supplement. |
| 3GPP specifications | `knowledge/ingest.py`'s `source_type='standard'` content; no paid equivalent to replace (not commercially paywalled) | Free to download, zero registration. **Not** public domain or open-licensed — copyright jointly held by the 3GPP Organizational Partners, each document carries a reproduction-restriction notice; internal ingestion for retrieval/citation (this repo's use case) is normal practice, bulk redistribution outside the repo's private DB is not. | No | [3gpp.org](https://www.3gpp.org/specifications-technologies) | Maps directly to `source_type='standard'`, which CONTEXT.md defaults to the authoritative-reference tier — correctly below manufacturer datasheets. Flag the separate FRAND-IPR patent-licensing caveat if guidance ever touches implementation, not just spec-reading. |
| ETSI standards | Same `source_type='standard'` gap — European/EN-track radio-equipment/EMC standards complementing 3GPP's cellular focus | Free PDF download (over 56,000 documents), no registration friction; only the editable Word version is access-restricted. Carries ETSI's own copyright/reproduction terms and (F)RAND patent terms — same internal-use posture as 3GPP. | No | [etsi.org standards search](https://www.etsi.org/standards/looking-for-an-etsi-standard) | Same `source_type='standard'` mapping as 3GPP; ETSI's EN-numbered radio-equipment/EMC standards (harmonized under the EU Radio Equipment Directive) fit this repo's antenna/RF-hardware compliance questions specifically. |
| FCC Rules — eCFR Title 47 | Same `source_type='standard'` gap — U.S. regulatory content (Part 15 unlicensed-device limits, EIRP caps, spurious emissions) | Public domain as a work of the U.S. Government — the strongest-license source in this report: free, zero registration, no reuse restriction, though text incorporating third-party material by reference can retain its own separate copyright. | No | [ecfr.gov/current/title-47](https://www.ecfr.gov/current/title-47) | Lowest-friction standards source to bulk-ingest given public-domain status. eCFR itself is not the official legal edition (GPO's Federal Register printing is authoritative) — fine for engineering reference, flag the distinction only if output is ever used for formal regulatory sign-off. |
| IEEE Standards / IEEE Xplore | **Not a source** — documented to correct a possible false "it's free too" assumption | Paid/paywalled, confirmed current — no broad free-developer tier exists analogous to Digi-Key/Mouser/Nexar. Institutional packages run roughly $54K–$84K+/year. | No | [ieee.org subscriptions](https://www.ieee.org/publications/subscriptions/index.html) | Do not build an ingestion path assuming free access. If the owner has employer/university access, individually licensed documents could still be ingested as `source_type='standard'` at the authoritative-reference tier, but content-license status would need separate tracking from `docs/LICENSE_MATRIX.md` (which tracks dependency licenses, not ingested-content licenses). |
| arXiv.org preprints | `source_type='paper'` content — a free complement to (not replacement for) IEEE's paywalled peer-reviewed literature | Free access, zero paywall/registration. Reuse licensing varies per paper — arXiv's default perpetual non-exclusive license lets arXiv distribute the article but doesn't itself grant downstream reuse rights beyond citation/summary use; check each paper's stated license (CC0/CC-BY/etc. where the author chose one). | No | [arXiv license info](https://info.arxiv.org/help/license/index.html) | Maps to `source_type='paper'`. arXiv papers are explicitly **not** peer-reviewed (only "superficial moderator review," per arXiv's own description) — per CONTEXT.md's authority rank being "overridable per document," an arXiv-sourced paper should have its authority rank overridden downward from the peer-reviewed-paper default unless independently corroborated, not silently inherit the same tier as a peer-reviewed IEEE paper. Genuinely useful because early-stage metamaterial/adaptive-EM-skin research often appears on arXiv well before (or instead of) a peer-reviewed venue. Ingestion is no longer a bare PDF handed to docling: `knowledge/sourcing/arxiv.py`'s `ingest_arxiv_paper` shells out to the vendored `.claude/skills/arxiv-doc-builder` skill's `convert-paper` CLI, which fetches LaTeX source (preferred) + PDF and converts via pandoc — preserving math/structure and pulling title/authors/date/DOI/categories/abstract from arXiv's own record — falling back to naive PDF text extraction only when no LaTeX source exists. |
| pandoc | `arxiv-doc-builder`'s own LaTeX→Markdown conversion dependency — not itself a data source | GPL-2.0-or-later (confirmed from `jgm/pandoc`'s own `COPYING.md`: GPLv2 text with the "or (at your option) any later version" clause; GitHub's own license detection on that repo independently reports `GPL-2.0`). | No | [pandoc.org](https://pandoc.org/) | Not a tool this repo scripts directly — it's what `.claude/skills/arxiv-doc-builder`'s `convert-paper` CLI shells out to for its LaTeX-source happy path (see the arXiv row above). Listed as its own row (not folded into that row) because it's a separately-licensed apt package this repo's Docker build actually installs (`apt-get install ... pandoc`, `Dockerfile`), the same reasoning this document already applies to gerbv vs. gerber2ems. Windows-only note: this CLI's fetch step also shells to `gunzip`/`file`, which are not reliably resolvable as Windows subprocesses even under Git Bash — like this report's other Linux-only solvers, treat this pipeline as Docker/Linux-only, not runnable directly on a Windows dev machine. |
| USPTO patent print PDFs (`image-ppubs.uspto.gov`) | `source_type='patent'` content — no paid equivalent to replace (a granted/published US patent is never paywalled) | No API key, no registration, no account: `GET https://image-ppubs.uspto.gov/dirsearch-public/print/downloadPdf/{number}` returns `application/pdf` directly, confirmed live during ticket #219's research against both a real grant (US12089385B2) and its real pre-grant publication (US 2022/0192066 A1). A granted US patent's specification text is conventionally treated as freely reproducible, but `knowledge/sourcing/patent.py` does not assume that for you — `license` is still a mandatory, caller-supplied argument, same as every other source in this table. | No | [image-ppubs.uspto.gov](https://image-ppubs.uspto.gov/dirsearch-public/print/downloadPdf/12089385) (live URL shape) | Maps to `source_type='patent'`, which already has its own `PATENT_AUTHORITY_RANK` (ticket #103) — this source adds an acquisition path, it does not touch ranking. `knowledge/sourcing/patent.py`'s `ingest_patent` reuses `.claude/skills/arxiv-doc-builder`'s generic (non-arXiv-specific) PDF extraction functions the same way `ingest_arxiv_paper` does, routing per-document on whether pypdf finds a real text layer — confirmed directly that this is NOT reliably predictable from "grant vs. publication" alone: both the real grant and the real pre-grant publication fetched during this research came back as scanned images with zero extractable characters. Google Patents (`patents.google.com`) was tried and rejected — it returns HTTP 503 to this network (anti-bot on datacenter IPs), not a genuine "not found". |
| poppler-utils | `pdf2image`'s external `pdftoppm`/`pdfinfo` binaries — not itself a data source | GPL-2.0-or-later (poppler is a fork of xpdf; Debian/Ubuntu's own `poppler-utils` package metadata reports `GPL-2.0+`). | No | [poppler.freedesktop.org](https://poppler.freedesktop.org/) | Not scripted directly by this repo — it's what `pdf2image.convert_from_path` (called by `.claude/skills/arxiv-doc-builder/arxiv_doc_builder/pdf_image_lib.py`, the vision-rendering path both arXiv's own PDF fallback and `knowledge/sourcing/patent.py`'s scanned-patent route use) shells out to. Listed as its own row for the same reason `pandoc` is: a separately-licensed apt package this repo's Docker build now actually installs (`apt-get install ... poppler-utils`, `Dockerfile`, added alongside `ingest_patent` in ticket #219 — confirmed missing from both this repo's Dockerfile and the sandbox that research ran in, where `pdf2image.convert_from_path` raised `PDFInfoNotInstalledError` before it, exactly the failure this package fixes). Same Linux-only caveat as `pandoc`/`file`/`gunzip`: not reliably resolvable as a Windows subprocess even under Git Bash. |

PyVISA-sim/PyVISA-py, Qucs-S, and the DEAP/pymoo/Optuna optimization-variety note also surfaced under this research category's own findings — merged into the Instrument backends, Circuit-level simulation, and "Already free" sections above respectively, rather than duplicated here. Likewise, Elmer FEM, Palace, and gprMax surfaced here too and are merged into the Full-wave EM section above; EMStudio surfaced here but is merged into the Geometry generation section above as the better thematic fit.

## Self-hosted / free LLM & embedding backends (alternative to the paid OpenAI API, per ADR-0004)

| Tool | Replaces/supplements | License | Hardware required? | Primary source | Fit for this repo |
|---|---|---|---|---|---|
| Ollama | OpenAI API — chat/completions (`knowledge/extraction_llm.py`'s `_extract_via_local`) and can also serve embeddings (`knowledge/embedding.py`'s `_embed_via_local`) | MIT (confirmed from `ollama/ollama`'s `LICENSE`) | No — genuinely usable CPU-only (8 GB RAM + AVX2 floor per Ollama's own docs); auto-detects and accelerates on an already-owned NVIDIA GPU (compute capability 5.0+), AMD ROCm, or Apple Metal. The owner's RTX 5080 is directly usable, no purchase implied. | [OpenAI compatibility docs](https://docs.ollama.com/api/openai-compatibility) | Ships a built-in OpenAI-compatible `/v1/chat/completions` (including `response_format` `json_object`/`json_schema`, matching `extraction_llm.py`'s JSON-mode call exactly), `/v1/embeddings`, and `/v1/models`. Set `LOCAL_LLM_BASE_URL=http://localhost:11434/v1`; `LOCAL_LLM_API_KEY` can be any placeholder (Ollama's shim ignores the value, mirroring `embedding.py`'s own `"unused"` fallback). `ollama pull gpt-oss:20b` (Apache-2.0) covers `LOCAL_EXTRACTION_MODEL`; `ollama pull nomic-embed-text` (Apache-2.0) or `bge-m3` (MIT) covers `LOCAL_EMBEDDING_MODEL` — one instance can serve both. Confirmed by reading `knowledge/extraction_llm.py` directly: it reads `os.environ['LOCAL_EXTRACTION_MODEL']`, but `.env.example` documents `LOCAL_EMBEDDING_MODEL` and not `LOCAL_EXTRACTION_MODEL` — a real, small config-doc gap worth closing alongside this integration, independent of which local server is chosen. |
| llama.cpp server (`llama-server`) | Same chat+embeddings pair as Ollama, lighter-weight/more-configurable (Ollama in fact embeds llama.cpp as its inference engine) | MIT (confirmed from `ggml-org/llama.cpp`'s `LICENSE`) | No — CPU-viable via GGUF-quantized weights by design; optional GPU offload (CUDA/Metal/ROCm/Vulkan/SYCL) reuses whatever GPU is already present. | [server README](https://github.com/ggml-org/llama.cpp/blob/master/tools/server/README.md) | `/v1/chat/completions`, `/v1/embeddings`, `/v1/models`, with `response_format` supporting both `json_object` and full schema-constrained `json_schema` — a strict superset of what `extraction_llm.py` needs. One `llama-server` process serves exactly one loaded model, so satisfying both the chat and embedding halves of ADR-0004's single `LOCAL_LLM_BASE_URL` contract means either loading a model capable of both, or running two `llama-server` instances on separate ports with their own base URLs — a small, real code change against ADR-0004's current single-URL plumbing, not just a config choice. |
| LM Studio | Same chat+embeddings pair, GUI-first — directly relevant because the owner's global Claude Code config already runs an LM Studio instance locally (serving `claude-context`'s bge-m3 embeddings at `localhost:1234`) | Proprietary closed-source freeware under LM Studio's own EULA — explicitly **not** OSI-approved; bars reverse engineering/decompiling/redistribution. Free-of-charge for personal and commercial/work use since July 2025 (a separate paid "Enterprise" tier for SSO/model-gating is irrelevant here). Free-of-charge, not open source — record distinctly from the MIT/Apache items in this table. | No — same underlying llama.cpp/MLX engine, same CPU-viable/optional-GPU profile. A live instance is already running on this machine for an unrelated project; reusing it here means deliberately sharing one server across two projects, not an accidental shared dependency. | [OpenAI-compat endpoint docs](https://lmstudio.ai/docs/app/api/endpoints/openai) | `/v1/chat/completions`, `/v1/embeddings`, `/v1/models`, including `json_schema`-constrained structured output shaped identically to OpenAI's own Structured Outputs API. Set `LOCAL_LLM_BASE_URL=http://localhost:1234/v1`. Marginal setup cost is near zero given the existing instance — load an extraction-capable chat model alongside the existing bge-m3 embedding model, or run a second LM Studio-managed server on a different port to keep this repo's config independent of `claude-context`'s. |
| Hugging Face Text Embeddings Inference (TEI) | The `LOCAL_EMBEDDING_MODEL` half of ADR-0004 specifically — a purpose-built high-throughput embedding server, no chat/completions at all | Apache-2.0 (confirmed from `huggingface/text-embeddings-inference`'s `LICENSE`). Note the license history precisely: TEI briefly shipped under HF's own non-OSI "HFOIL" license in 2023 before HF's own GitHub issue #232 (April 2024) announced the move to Apache-2.0 as permanent; current releases (v1.9 at time of writing) are confirmed Apache-2.0. | No — dedicated CPU-only Docker images (`cpu-1.9`, ONNX/Intel backend) exist alongside GPU images, since embedding inference is far cheaper than chat-LLM inference. | [quick tour](https://huggingface.co/docs/text-embeddings-inference/en/quick_tour) | Documented OpenAI-compatible `POST /v1/embeddings` route alongside its native, lower-overhead `/embed` route. `docker run -p 8081:80 ghcr.io/huggingface/text-embeddings-inference:cpu-1.9 --model-id BAAI/bge-m3` (MIT model, same one the owner's global `claude-context` setup already uses) serves embeddings on a distinct port from any chat backend. Since ADR-0004 currently plumbs one `LOCAL_LLM_BASE_URL` through both `knowledge/embedding.py` and `knowledge/extraction_llm.py`, pairing TEI (port 8081) with a chat server (port 8080/11434) means either dedicating `LOCAL_LLM_BASE_URL` to one of them and adding a second, embedding-specific base-URL env var for the other, or keeping Ollama/LM Studio as the single combined server and treating TEI as optional. Cannot itself cover `_extract_via_local` at all — must be paired with a chat-capable server for that half. |
| sentence-transformers | Not itself an OpenAI-compatible server — the reference Python library the local embedding models above (BGE-M3, nomic-embed-text) are published for, and effectively what TEI/Ollama load under the hood | Apache-2.0 (confirmed from `UKPLab/sentence-transformers`'s `LICENSE`) | No — PyTorch-backed, CPU or GPU depending on the installed `torch` build. | [repo](https://github.com/UKPLab/sentence-transformers) | Not a direct `LOCAL_LLM_BASE_URL` substitute — would only satisfy ADR-0004's HTTP + OpenAI-wire-format contract wrapped in a small custom FastAPI/Flask shim this repo doesn't have and that TEI already provides for the same models. Realistic use is as a library dependency for offline/batch tooling (e.g. a standalone re-embedding script under `knowledge/`), not as the server behind `LOCAL_LLM_BASE_URL`. |

## Recommended defaults — install with no tangible equipment required

None of the items below should become a hard `[project.dependencies]` default. That matches this repo's own established pattern: `pyaedt` and `pyvisa` are already gated behind `[project.optional-dependencies]` extras (`hfss`, `measurement`) specifically because most installs and CI never touch them — the same logic applies to every simulation/geometry/embedding library below, none of which the core agent loop needs to import to run.

**Extend the existing `measurement` extra** (pip-installable, zero hardware to install):
- `pyvisa-sim` (MIT) — needed to exercise `measurement/*.py` end-to-end with zero lab hardware. Also needs a repo-specific YAML fixture matching the real SCPI vocabulary (custom-build item, see below) before it's more than a smoke test.
- `pyvisa-py` (MIT) — the documented free alternative to NI-VISA/Keysight IO Libraries. Installs today with zero hardware; only becomes useful once real hardware is attached (see "still requires payment or physical equipment" below).

**New optional extra**, e.g. `geometry` (pip-installable, zero hardware):
- `CSXCAD` (LGPLv3) — `pip install CSXCAD`
- `gdstk` (BSL-1.0) — `pip install gdstk`
- `gerber2ems` (Apache-2.0) — `pip install gerber2ems`; also needs the external `gerbv` binary as a manual-install companion, same pattern as the existing NEC2++/openEMS binaries
- `pyems` (GPL-3.0) — optional, reference-quality per the fidelity note above; note its GPL-3.0 copyleft vs. CSXCAD's LGPL if both are pulled in

**Manual binary/source installs** (this repo's existing convention — documented in README as manual, same as NEC2++/openEMS today, not a `pyproject.toml` extra):
- MEEP (GPLv2), Palace (Apache-2.0), OpenParEM (GPLv3), gprMax (GPLv3, though it's pip-installable and could go under the `geometry`/a new `simulation-extra` extra instead), ngspice (mixed BSD/MIT/GPL/LGPL per-subtree), Xyce (GPLv3), FreeCAD (LGPL-2.1), KiCad (GPL-3.0-or-later)
- Ollama (MIT) and llama.cpp/`llama-server` (MIT) as standalone local-LLM servers; Hugging Face TEI (Apache-2.0) as a Docker image
- For all local-LLM/embedding servers: **no new pip package is needed** — the `openai`-compatible client code path ADR-0004 already requires for OpenAI itself works unmodified against any OpenAI-wire-format server. Just install the binary/image and point `LOCAL_LLM_BASE_URL` (and, if TEI is split onto its own port, a second embedding-specific base-URL env var — a small real code change against ADR-0004's current single-URL contract) at it.
- Elmer FEM (ElmerSolver + ElmerGrid, plus Gmsh) and Qucs-S/`qucsator_rf`: install these to exercise `simulation/elmer.py` and `simulation/qucs.py`, both of which are now built — this line previously said "only if their respective adapters actually get built", which is no longer true. Qucs-S's flagged CLI/headless contradiction was resolved in the course of building that adapter: the binary `simulation/qucs.py` shells out to is named `qucsator_rf`, not the bare `qucsator` the upstream project is colloquially called (see README's own note).

**Free-of-charge but not open source** — usable today, but track distinctly in `docs/LICENSE_MATRIX.md` rather than alongside the MIT/Apache/GPL rows:
- LM Studio (proprietary EULA, free-of-charge since July 2025) — already running on this machine for an unrelated project; reusable here with its own model(s) loaded
- Sonnet Lite (proprietary freeware, Windows-only, 32 MB registered cap) — manual, out-of-band cross-check only, not automatable
- LTspice (proprietary freeware, Analog Devices EULA) — useful for its vendor model library; ngspice/Xyce remain the preferred adapter targets since they're OSS and natively headless

**External accounts / free-tier APIs** — no install, configure via `.env`:
- Digi-Key Product Information API v4, Mouser Search API, Nexar API — free developer registration each; feed `ingest_document(source_type='datasheet')`
- 3GPP specifications, ETSI standards, FCC eCFR Title 47 — free downloads, no account; feed `ingest_document(source_type='standard')`
- arXiv.org preprints — free, no account; feed `ingest_document(source_type='paper')`, with authority rank explicitly overridden downward from the peer-reviewed default (see the data-sources section above)

**Still requires payment or physical equipment** — kept separate so this is the one place that distinguishes the two:
- A real VNA, spectrum analyzer, signal generator, and power meter — no free/OSS substitute exists for the physical measurement itself. `pyvisa-sim` only exercises the adapter's code path, never real RF measurement.
- NI-VISA / Keysight IO Libraries — proprietary VISA runtime; `pyvisa-py` is the free substitute once real hardware is attached.
- Ansys AEDT / HFSS — paid, licensed-workstation-only boundary (`simulation/hfss.py`); intentionally not replaced, per this repo's own stance.
- Keysight ADS — paid; ngspice/Xyce cover part of its circuit-level role (see above), not a full replacement — no free Momentum-class planar EM solver is among the free/OSS options surveyed.
- IEEE Xplore standards/papers — paid, no broad free-developer tier; listed to correct a possible false "it's free too" assumption.
- The GPU-dependent features specifically — openEMS-CUDA's accelerated engine and gprMax's optional CUDA path both need an NVIDIA GPU. The owner's existing RTX 5080 already satisfies this, so it is not a new purchase, but it's the one place in this report where a feature is gated on tangible hardware at all, worth naming explicitly rather than silently folding into the "no hardware required" rows above.

## What has to be custom-built

Synthesized against CONTEXT.md's five explicitly open gaps. For each: whether a free/OSS piece above closes it outright ("wire-up": install/configure and write a thin adapter), or whether the core work has no off-the-shelf substitute and must be written as new domain logic in this repo ("custom"), and which module it belongs in.

**RF knowledge-base ingestion pipeline's external content sourcing** — mostly wire-up. Digi-Key/Mouser/Nexar API clients are thin new modules (e.g. `knowledge/sourcing/digikey.py`, `mouser.py`, `nexar.py`) that fetch a datasheet URL and hand it to the existing `ingest_document(source_type='datasheet')` pipeline — no new ingestion logic needed. 3GPP/ETSI/FCC/arXiv are simpler still: download plus `ingest_document(source_type='standard'|'paper')`. The one genuinely custom piece: an authority-rank override rule for non-peer-reviewed sources like arXiv — CONTEXT.md already defines authority rank as "overridable per document," but nothing currently sets it downward automatically; a small rule in the ingestion path, not a new library.

**Component/manufacturer intelligence** — wire-up for the fetch side (the same API clients above), custom for cross-source identity resolution: reconciling the same physical part across Digi-Key/Mouser/Nexar responses into one `(manufacturer, part_number)` row, honoring CONTEXT.md's Component vocabulary (package and tape-and-reel suffix are part of identity, not a variant field). No library in this research does this reconciliation for this repo's exact identity model — belongs in `knowledge/` (e.g. `knowledge/component_resolution.py`), built on the existing `component_schema.py`.

**Antenna geometry generators (metamaterial unit-cell / conformal antenna)** — mixed, leaning custom for the domain-specific core. Wire-up: gdstk gives array/polygon primitives for planar unit-cell layout; CSXCAD gives the `<Polygon>` primitive `simulation/openems.py`'s `_primitive_xml()` doesn't emit today (Box/Cylinder only) — extending that function with a `polygon` branch is small and well-scoped; EMStudio's Element Designer already wires conventional wire/patch/Yagi/LPDA synthesis into openEMS/NEC2/Elmer/Palace. Custom: no tool surveyed synthesizes a metamaterial unit cell's actual geometry (SRR gap/split-ring dimensions, Jerusalem-cross arm lengths) from a target effective-permittivity/permeability requirement, or maps a flat unit-cell layout onto a curved conformal host surface — EMStudio's own antenna families are explicitly conventional, not metamaterial. This parametrization math belongs in `designs/` or a new `simulation/geometry_generators.py`, built on gdstk (planar tiling) + CSXCAD (3D primitive emission) + FreeCAD (curved-surface mapping) as building blocks, not replacing them.

**HFSS/ADS adapters** — split by target, both now closed. HFSS:
`simulation/hfss.py` is a real, working `HfssSimulator` adapter (geometry,
setup/solve, extraction, archiving), gated by
`check_hfss_workstation_confinement` — implemented, not a stub, just
license-confined; not a free/OSS gap, and not on this repo's default
roadmap path per ADR-0012. ADS: `simulation/ngspice.py` and
`simulation/xyce.py` now cover the nearest free/OSS substitutes for its
circuit-level role, following the exact `Simulator` ABC + subprocess
pattern `simulation/nec2pp.py`/`simulation/openems.py` already use.
`simulation/qucs.py` (Qucs-S/`qucsator_rf`) is also now built. Custom:
closed-form Chebyshev/Butterworth/Bessel filter-prototype synthesis
(order/ripple/cutoff → g-value table → ladder network) — confirmed absent
from both scikit-rf and this repo's `rf_tools/` by direct grep.
Deterministic, well-specified math; belongs in a new
`rf_tools/filter_synthesis.py` built on scikit-rf's existing
`Circuit`/`Media` primitives, not a new external dependency.

**Instrument (VISA/SCPI) integration beyond current scaffolding** — mostly wire-up for exercising the existing adapters: pyvisa-sim needs zero adapter code changes, just the `measurement` extra, a `PYVISA_LIBRARY=@sim` (or custom YAML resource string) setting, and an explicit `approval_callback` in the calling test. Custom: (a) a repo-specific YAML fixture (e.g. `tests/fixtures/pyvisa_sim_vna.yaml` and equivalents for the other three adapters) whose `dialogues:`/`properties:` entries match this repo's actual `DEFAULT_SCPI_COMMANDS` — nothing upstream provides this; (b) real hardware integration stays blocked on the physical instruments themselves (payment) and on the human-facing approval UI that `measurement/base.py`'s own docstring already says doesn't exist yet — no free/OSS tool in this research addresses that gap, since it isn't a licensing problem, it's unwritten application code.

**Simulation/measurement correlation** — entirely custom. No tool surveyed across any of the six categories compares a `SIMULATED`-provenance result against a `MEASURED`-provenance result the way this repo's evidence hierarchy requires. OpenParEM/Palace/MEEP/gprMax improve what's available to correlate against (independent-method cross-checks alongside NEC2++/openEMS), and pyvisa-sim/GNU Radio let the measurement side be exercised without a bench, but the comparison logic itself — matching a `SimulationResult` to a VNA-adapter `Network`, computing agreement/disagreement, deciding what "correlated" means for this repo's evidence hierarchy — is new code. Most naturally belongs in `simulation/` or a new top-level `correlation/` module referencing both `simulation/` and `measurement/` result types.
